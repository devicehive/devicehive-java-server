codec:
  class: org.redisson.codec.SerializationCodec
singleServerConfig:
  address: "redis://${REDIS_MASTER_HOST:-localhost}:${REDIS_MASTER_PORT:-6379}"
  idleConnectionTimeout: 10000
  connectTimeout: 10000
  timeout: 3000
  retryAttempts: 3
  retryInterval: 1500
  subscriptionsPerConnection: 5